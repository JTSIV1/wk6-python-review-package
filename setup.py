from setuptools import setup, find_packages

setup(
    name='ai_ppp_python_review_solutions',
    version='1.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies your package requires
    ],
)